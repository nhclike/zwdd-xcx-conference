export interface PickChatParams {
    corpId: string;
    isAllowCreateGroup?: boolean;
    filterNotOwnerGroup?: boolean;
}
export interface ChatResults {
    chatId: string;
    title: string;
}
export default function pickChatByCorpId(params: PickChatParams): Promise<ChatResults>;
