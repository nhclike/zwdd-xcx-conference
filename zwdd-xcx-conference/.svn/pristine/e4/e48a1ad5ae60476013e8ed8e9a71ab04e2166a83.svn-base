/**
 * 比较客户端版本号大小 目前出现4位版本号 有些 JSAPI 需要首先判断客户端的版本号才能调用
 * @param version 当前客户端版本号
 * @param baseVersion  最小支持版本号
 * @return -1 表示小于当前版本 1 表示大于当前版本
 */
declare function compareVersion(version?: string, baseVersion?: string): number;
export default compareVersion;
